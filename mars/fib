PROC_FIB:
#int fib(int n)
	#if(n<3)
	slti $t0,$a0,3
	beq $t0,$zero,PROC_FIB_FIM_IF
		#return 1
		
		#retorno é armazenado nos v's
		addi $v0,$zero,1
		jr $ra
	PROC_FIB_FIM_IF:
	#return fib_aux(1,1,n-2)
	#chamada de funcao
	addi $sp,$sp,-4
	sw $ra,4($sp)
	#argumentos
	addi $a2,$a0,-3
	addi $a0,$zero,1
	addi $a1,$zero,1
	jal FIB_REC
	#add $v0,$zero,$v0 // nao precisa dessa instrução
	
	#restaura a pilha
	lw $ra,4($sp)
	addi $sp,$sp,4
	jr $ra
	
#}
FIB_REC:

#int fib_rec(int fibNMenos1,intFibMenos2, int falta){
	#if(falta==0)
	bne $a2,$zero,FIB_REC_FIM_IF
		#return fibmenos1+fibmenos2
		add $v0,$a0,$a1
		jr $ra
		
	FIB_REC_FIM_IF:
	#return fib_rec(fibmenos2, fibmenos1 + fibmenos2, falta-1);
	addi $sp,$sp,-4
	sw $ra,4($sp)
	add $t0,$zero,$a0
	add $a0,$zero,$a1
	add $a1,$t0,$a1
	addi $a2,$a2,-1
	jal FIB_REC
	#add $v0,$zero,$v0 //nao precisa
	
	lw $ra,4($sp)
	addi $sp,$sp,4
	jr $ra
#}